export class AppModel {
    token;

    state = 'signup'; // определяет в каком состоянии находится приложение (signUp(регистрация), auth(авторизован), login)
    //коротко - состояние приложения

}

